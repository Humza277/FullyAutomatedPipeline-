# installing jenkins on 16.04




# ssh into the ubuntu ec2 server
    ssh -i ~/.ssh/awsaccessjenkins.pem ubuntu@52.212.115.133

# Update the os
sudo apt update


# add repo to system
wget -q -O - https://pkg.jenkins.io/debian/jenkins-ci.org.key | sudo apt-key add -

# echo the repo to the souces list
echo deb https://pkg.jenkins.io/debian-stable binary/ | sudo tee /etc/apt/sources.list.d/jenkins.list

# run update to get the new repo
sudo apt-get update

# Install Jenkins + Java
sudo apt-get install jenkins

# Starting Jenkins

# start Jenkins
sudo systemctl start jenkins

# check status of Jenkins
sudo systemctl status jenkins

navigate to
http://52.212.115.133:8080/

Jenkins should be running

now add the docker plugins

git integration plugs and icon shim

# agent adding

now we need to set the agent to run build on
to do this we need to create a new ec2 instance for the worker node
set it up

add port 8080 to the ingree groups and put the source as the main jenkins ec2
do the same to the main with the source being the slave ip


# Worker Node

ssh -i ~/.ssh/awsaccessjenkins.pem ubuntu@34.245.101.40

cd /opt/
mkdir jenkins folder
sudo su
sudo adduser jenkins
    enter unix password : add
    fullname- blank
    room number - blank
    work - blank
    home -blank
    other -blank
cd
sudo apt install default-jdk
sudo apt install default-jre
sudo apt upgrade

sudo su
cd /opt/jenkins
# To run the agent
java -jar agent.jar -jnlpUrl http://52.212.115.133:8080/computer/Agent%20Slave/slave-agent.jnlp -secret @secret-file -workDir "/opt/jenkins"







# Jenkins

add java script to it #


BOOBS aaaa


java -jar agent.jar -jnlpUrl http://52.212.115.133:8080/computer/Agent%20Slave/slave-agent.jnlp -secret 6866cd01a5727b100524cb4a16eae62ff026da52b51be0dc5fe9e30f73857bee -workDir "/opt/jenkins"



Install npm on thw worker node

curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash -
sudo apt-get install nodejs -y


# Add the webhook to the github repo
http://52.212.115.133:8080/github-webhook/